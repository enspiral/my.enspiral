<h1>People</h1>

<%= link_to 'Add Person', new_admin_person_path %>

<p>
Total positive accounts : <strong><%= number_to_currency(@positive_total) %></strong><br/>
Total negative accounts : <strong><%= number_to_currency(@negative_total) %></strong><br/>
There should be <strong><%= number_to_currency(@balance) %></strong> in the Staff Account.<br/>
</p>


<table id="people_list" class="tablesorter">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Account</th>
      <th>Pending</th>
      <th>Date Created</th>
    </tr>
  </thead>
  <tbody>
  <% for person in @people %>
    <tr>
      <td><%= link_to display_gravatar(person, 32, :border => true), person %></td>
      <td><%= link_to person.name, admin_person_path(person) %></td>
      <td><%= number_to_currency person.account.balance %></td>
      <td><%= number_to_currency person.pending_total %></td>
      <td><%= person.created_at.strftime '%d %b %Y %I:%M %p' %></td>
    </tr>
  <% end %>
  </tbody>
</table>
