<h1>Service Categories</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @service_categories.each do |service_category| %>
  <tr>
    <td><%= service_category.name %></td>
    <td><%= link_to 'Edit', edit_admin_service_category_path(service_category) %></td>
    <td><%= link_to 'Destroy', admin_service_category_path(service_category), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Service Category', new_admin_service_category_path %>
