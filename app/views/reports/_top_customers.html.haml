- top_customers.each_with_index do |top_customer, index|
  %table.table.table-striped.tablesorter#details
    %thead
      %tr
        %th Name
        %th= date[index]
    %tbody
      - top_customer.each do |name, amount|
        %tr
          %td= name
          %td= number_to_currency amount