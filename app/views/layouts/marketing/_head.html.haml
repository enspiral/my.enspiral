%head
  %meta{:charset => "utf-8"}

  -#
    Always force latest IE rendering engine (even in intranet) & Chrome Frame
    Remove this if you use the .htaccess
  %meta{:content => "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  %title= "Enspiral #{!@title.blank? ? " | " + @title : "| Social entrepreneurs cultivating a thriving future"}"
  %meta{:content => "", :name => "description"}/
  %meta{:content => "", :name => "author"}/

  = favicon_link_tag
  = stylesheet_link_tag    "marketing"
  = javascript_include_tag "modernizr-2.5.3"

  = csrf_meta_tag

  -# Analytical
  - analytical.identify(current_user.id, :email=>current_user.email) if current_user
  = raw analytical.head_append_javascript  

