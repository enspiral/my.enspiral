- title("Your Enspiral work capacity.")

%table
  %tbody
    %thead
      %td 
        %strong Capacity
      %td This Week
      %td Next Week
      %td Week #{(Date.today + 2.weeks).strftime('%W')}
      %td Week #{(Date.today + 3.weeks).strftime('%W')}
      %td Week #{(Date.today + 4.weeks).strftime('%W')}
    %tr
      %td Your Availability:
      -@availabilities.each do |availability|
        %td #{availability.time}
      %td= link_to 'Modify', batch_edit_staff_availabilities_path, :method => :get
    %tr
      %td Your Total Bookings:
      -@total_hours_booked.each do |key, total_hours|
        %td #{total_hours}

    %tr
      %td &nbsp;
    %tr
      %td 
        %strong Project

    -@projects.each do |project|
      %tr
        %td= project.name
        - if @project_bookings
          - project_bookings = @project_bookings[project.id]
          - for project_booking in project_bookings do
            %td #{project_booking.time}
        %td= link_to 'Modify', batch_edit_staff_bookings_path(:id => project.id), :method => :get

