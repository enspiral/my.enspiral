%h2 Edit Company Membership

=simple_form_for(@membership, url: show_path(@membership)) do |f|
  .single-checkbox
    =f.input :admin
  =f.input :role, :collection => CompanyMembership::ROLES
  =f.submit
