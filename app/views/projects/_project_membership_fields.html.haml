.nested-fields
  =f.input :person_id, collection: Person.active.all, input_html: {class: 'czn-select'}
  =f.input :is_lead, label: 'Project Lead?'
  =f.input :role
  =link_to_remove_association 'Remove', f, class: 'btn'

