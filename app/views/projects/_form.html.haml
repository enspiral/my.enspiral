%h1.bottom_margin Project Details
= simple_form_for (@project) do |f|
  =f.error_messages
  =f.input :name, label: 'Title'
  =f.input :description
  .control-group
    =f.label :customer_id
    =f.grouped_collection_select :customer_id, current_person.companies.all, :customers, :name, :id, :name, {}, {class: 'czn-select'}
    =link_to 'New Customer', new_customer_projects_path

  =f.input :status, as: :select, collection: ['active', 'inactive'], include_blank: nil
  =f.input :due_date, as: :string, input_html: {class: 'datepicker'}

  .form-actions
    = f.submit class: 'btn btn-primary'
